{
  "name": "server",
  "scripts": {
    "dev": "bun run --hot src/server.ts",
    "db:generate": "bunx --bun drizzle-kit generate",
    "db:migrate": "bun run src/core/utils/database/migration.ts",
    "db:seed": "bun run src/core/utils/database/seeder.ts",
    "db:studio": "bunx --bun drizzle-kit studio"
  },
  "dependencies": {
    "drizzle-orm": "^0.44.6",
    "groq-sdk": "^0.33.0",
    "hono": "^4.9.12",
    "pino": "^10.0.0",
    "pino-pretty": "^13.1.2",
    "postgres": "^3.4.7",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.5"
  }
}